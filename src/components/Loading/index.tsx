import { LoadingContainer, LoadingIcon } from "./styled";

const Loading = (): JSX.Element => {
  return (
    <LoadingContainer>
      <LoadingIcon />
    </LoadingContainer>
  );
};

export default Loading;
